import 'package:flutter/material.dart';

import 'ListView/listView.dart';
/*
Widget streamBuilder(stream, list, bloc) {
  return StreamBuilder(
    stream: stream,
    builder: (context, snapshot) {
      return list.length != 0 ? listView(bloc, bloc.planList[bloc.planList.length - 1]) : Container();
    },
  );
}
*/
